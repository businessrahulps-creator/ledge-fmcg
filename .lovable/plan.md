

# Primary Color Upgrade: #4338CA Indigo

## Change

Update `--primary` in `src/index.css` from `222 47% 42%` to the HSL equivalent of #4338CA → `245 75% 52%`.

Also update the ring token (`--ring`) to match, and the dark mode primary from `222 60% 58%` to `245 70% 65%` (lighter indigo for dark surfaces).

## Files Changed

| File | Change |
|------|--------|
| `src/index.css` | Update `--primary`, `--ring` in `:root` and `.dark` blocks (~4 lines) |

**1 file, 4 token updates. Every button, link, active state, progress bar, and accent across the entire app updates automatically.**

