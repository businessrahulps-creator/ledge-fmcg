
The preview is showing an old build because the dev server build failed earlier (the "Unterminated regular expression" error in `Billing.tsx`). When Vite fails to compile, the browser keeps serving the last successfully built version — that's why you're seeing the "old build."

Let me verify the current state of the file and dev server logs before proposing a fix.
